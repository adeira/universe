// @flow

import { create } from '@storybook/theming';

export default (create({
  base: 'light',
  brandTitle: 'WLCM Design Storybook',
  brandUrl: 'https://github.com/adeira/sx-design',
}) /*: $FlowFixMe */);
