// @flow

import type { SheetDefinitions } from '../../create';

export default ({
  redStyle: {
    color: 'red',
  },
  blueStyle: {
    color: 'blue',
  },
  greenStyle: {
    color: 'green',
  },
}: SheetDefinitions);
